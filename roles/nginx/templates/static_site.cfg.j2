server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /var/www/{{ domain }};
        server_name {{ domain }};
        location / {
                try_files $uri $uri/ =404;
        }
}